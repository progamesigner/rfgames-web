{{- $context := 0 | index . -}}
{{- $kind := 1 | index . | string | lower -}}

{{- $ratingMaps := "build-ratings" | index $context.Site.Data -}}
{{- $ratingTitles := "titles" | index $ratingMaps -}}

{{- $title := $kind | index $ratingTitles | default "Unknown" -}}

{{- return $title -}}
