{{- $context := 0 | index . -}}

{{- $data := slice $context "trait-slug-to-id" | partial "functions/gw2-data-load.dict" -}}

{{- if $data | len | ge 0 -}}
    {{- $traits := slice $context | partialCached "apis/gw2-get-traits.dict" -}}

    {{- range $traits -}}
        {{- $slug := "name" | index . | slice $context | partial "functions/gw2-data-slugify.string" -}}
        {{- if $slug | index $data | not -}}
            {{- $data = "id" | index . | dict $slug | merge $data -}}
        {{- end -}}
    {{- end -}}
{{- end -}}

{{- return $data -}}
